language: rust
rust:
  - nightly
  - beta
  - stable

matrix:
  allow_failures:
    - rust: nightly

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y libapt-pkg-dev build-essential

dist: trusty
